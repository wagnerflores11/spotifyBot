[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
# Foco em erros reais (pyflakes + erros de sintaxe), não em estilo cosmético.
select = ["E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
